# Neon Postgres connection string (provision via Vercel Marketplace)
DATABASE_URL=postgres://user:pass@host/db?sslmode=require

# Google API key with Sheets API enabled (Cloud Console → APIs & Services)
# Sheets must be set to "Anyone with the link can view"
GOOGLE_API_KEY=AIza...
